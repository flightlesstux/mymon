# Per-source overrides. Keys are Source.name values. Omitted sources run with module defaults.
#   enabled: false      -> never scheduled
#   interval: <seconds> -> override polling interval
sources: {}
