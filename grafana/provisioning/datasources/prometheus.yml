apiVersion: 1

datasources:
  - name: MymonPrometheus
    uid: mymon-prom
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false
    jsonData:
      httpMethod: POST
      timeInterval: 30s
